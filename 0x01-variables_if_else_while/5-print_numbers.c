#include <stdio.h>
/**
 *  * main - Prints all single Entry point
 *   *
 *    * Return: Always 0 (Success)
*/
int main(void)
{int num;

	for (num = 0 ; num <= 9 ; num++)
		printf("%d", num);
	puts("");
	return (0);
}
